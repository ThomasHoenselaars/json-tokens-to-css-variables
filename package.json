{
  "name": "json-tokens-to-css-variables",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "style": "src/data/design-tokens.css",
  "unpkg": "src/data/design-tokens.css",
  "scripts": {
    "start": "nodemon -e ts --exec 'npm run serve'",
    "serve": "tsc && node dist/index.js",
    "build": "tsc",
    "publish": "npm run serve && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thomas Hoenselaars<thomas@angistudio.com>",
  "license": "MIT",
  "dependencies": {
    "nodemon": "^2.0.16"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.7.4"
  }
}
